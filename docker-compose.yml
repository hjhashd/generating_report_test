version: '3.8'

services:
  app:
    # 基础构建配置
    build: .
    image: langextract-app:${TAG:-latest}
    
    # 基础环境变量
    environment:
      - PORT=34521
      - PYTHONUNBUFFERED=1

    # 默认网络配置
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
